# v0.5.0 歌曲列表 / Suno WAV / AI 消痕工作流

## 本版目标

解决 v0.4.0 提交后只能看到“当前任务”、没有持久化歌曲列表的问题，并把 Suno 下载与 AVR N19 AI 消痕串成一条完整工作流。

## 已实现

- ✅ 新增独立“歌曲列表”页面。
- ✅ 每次原创提交成功后，把 Suno 返回的每个 clip 当成一首独立歌曲记录；通常一次提交返回两个版本，因此列表会出现版本 1 / 版本 2 两行。
- ✅ 歌曲列表保存在 Electron `userData/song-library-v1.json`，关闭软件、重新打开后仍然存在。
- ✅ 保存字段：歌名、完整歌词、风格提示词、账号、模型、人声、Weirdness、Style Influence、clip ID、版本号、提交时间。
- ✅ 记录 Suno 生成状态：submitted / complete / failed 等。
- ✅ 记录 WAV 下载状态：未下载 / 下载中 / 已下载 / 失败。
- ✅ 记录 AI 消痕状态：未处理 / 等待处理 / 处理中 / 已完成 / 失败。
- ✅ 记录本地保存状态：未保存 / 保存中 / 已保存 / 失败。
- ✅ 列表支持手动刷新 Suno 状态。
- ✅ 仍在生成的歌曲每 10 秒自动刷新一次状态。
- ✅ 可从歌曲列表直接勾选一首或多首已完成歌曲。
- ✅ “对选中歌曲 AI 消痕”会按顺序处理。
- ✅ 可选择统一作品保存根目录；默认：`文档/SunoOriginalStudio作品`。
- ✅ 每个 clip 建独立目录，避免同名歌曲或两个版本互相覆盖。
- ✅ 每首歌保存 `歌词.txt`。
- ✅ 使用当前歌曲对应 Suno 账号的登录 Session 请求 Suno WAV 转换。
- ✅ WAV 请求流程：`POST /api/gen/{clip_id}/convert_wav/` → 轮询 `GET /api/gen/{clip_id}/wav_file/`。
- ✅ 将 Suno 返回的 WAV 保存为 `<歌名>-Suno原始.wav`。
- ✅ 下载完成后自动执行 AVR 1.77.0 精确 N19 链路。
- ✅ N19 最终文件保存为 `<歌名>-消痕-N19.wav`。
- ✅ 可从列表直接打开 Suno 作品页。
- ✅ 已生成本地目录的歌曲可直接打开本地目录。

## 每首歌曲最终目录

示例：

```text
SunoOriginalStudio作品/
└─ 凌晨四点的便利店-V1-a1b2c3d4/
   ├─ 歌词.txt
   ├─ 凌晨四点的便利店-Suno原始.wav
   └─ 凌晨四点的便利店-消痕-N19.wav
```

其中 `Suno原始.wav` 是通过 Suno 当前账号的 WAV 转换/下载接口取得的 WAV，不使用 MP3 转 WAV 冒充 Suno WAV；如果当前 Suno 账号没有 WAV 导出权限，任务会明确记录为下载失败，不静默降级成 MP3。

## AVR N19 处理顺序

歌曲列表工作流使用与独立 AI 消痕页面相同的精确链路：

```text
Suno WAV
↓
FFmpeg 解码：44.1kHz / PCM16 / Stereo
↓
SoX 节点 1
↓
FFmpeg loudnorm：I=-15 / TP=-1.5 / LRA=11
↓
FFmpeg 节点 9：Rubber Band pitch=0.975 + EQ + Echo + Compressor + Limiter
↓
组合后处理
↓
48kHz / PCM16 / Stereo WAV
```

SoX、FFmpeg、Rubber Band 工具链仍执行 v0.4.0 的 SHA-256 精确校验，不允许静默退化成 FFmpeg-only 兼容方案。

## 后续仍未完成

- ⬜ 从 Suno 账号自动同步历史歌曲到本地歌曲列表。
- ⬜ 删除歌曲列表记录。
- ⬜ 搜索、筛选、排序。
- ⬜ 失败任务一键重试。
- ⬜ AI 消痕任务取消与断点恢复。
- ⬜ Excel 批量原创直接进入歌曲列表。
- ⬜ 批量原创完成后自动下载 + 自动 N19，无需手工勾选。
- ⬜ 歌曲列表导出 Excel。

## 开发优先级

当前顺序：

1. 先实机验证“提交 → 歌曲列表 → 生成完成 → Suno WAV → N19 → 本地歌词/WAV”完整闭环。
2. 再做自动下载 / 自动 N19。
3. 再做 Excel 批量原创与多账号调度。
