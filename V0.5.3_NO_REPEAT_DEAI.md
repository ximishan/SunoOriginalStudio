# v0.5.3 — 已消痕歌曲禁止重复处理

## 目标

歌曲列表中的歌曲一旦 `deaiStatus = complete`，后续无论用户是否再次选中、旧界面是否残留选中状态、或前端再次把该 `clipId` 传给后端，都不得再次执行 Suno WAV 下载和 AVR N19 AI 消痕。

## 实现

1. 后端 `processSelectedSongs()` 在任何下载或 N19 处理之前检查 `deaiStatus`。
2. 若状态已经是 `complete`：
   - 直接跳过；
   - 不重新请求 Suno WAV；
   - 不重新下载 WAV；
   - 不再次运行 SoX / Rubber Band / FFmpeg；
   - 保留现有 `localDir`、原始 WAV、N19 WAV 和歌词路径；
   - 返回 `skipped: true` 与 `reason: already_processed`。
3. 前端歌曲列表同时禁止勾选 `deaiStatus = complete` 的歌曲。
4. “全选可处理歌曲”自动排除已经完成 AI 消痕的歌曲。
5. 批量处理结果分别统计：本次新处理成功、已消痕自动跳过、失败。

## 状态原则

`deaiStatus = complete` 是防重复处理的权威状态。即使本地文件后来被手动移动或删除，也不会因为再次勾选而重新消痕；需要重新处理时应由未来单独的“重新处理/重置状态”功能显式触发，而不是隐式重复执行。

## 保留功能

本次不修改：
- Suno 登录持久化
- 多账号 Session
- 原创提交
- 排除风格 `negative_tags`
- 歌曲列表
- Suno WAV 下载
- AVR 1.77.0 原版完整 N19（SoX + Rubber Band + FFmpeg）链路
