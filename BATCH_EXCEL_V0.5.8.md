# v0.5.8 Excel 批量原创

## 1. 功能目标

v0.5.8 增加 Excel 顺序批量原创：选择一个 `.xlsx` / `.xls` 文件，一行代表一首歌曲，程序读取第一张工作表，先做参数校验和预览，再按照 Excel 原顺序逐首调用现有 Suno 原创提交链路。

每首歌曲提交结束后，会等待指定秒数再进入下一首。默认等待 **20 秒**，软件全局可配置 **5-300 秒**，Excel 的“提交间隔秒”也可以为单独一首覆盖全局值。这个间隔仅用于控制请求频率，不用于绕过 Suno 的限制、人机验证或其他平台规则。

---

## 2. 正式 Excel 模板

仓库固定保存一份可直接填写的正式模板：

[`templates/SunoOriginalStudio批量原创模板_v0.5.8.xlsx`](templates/SunoOriginalStudio批量原创模板_v0.5.8.xlsx)

模板 SHA-256：

```text
526f699c5ce1f709683c0524074f4125f5d74241c2ec641f868f889bcb7afba5
```

模板包含两个工作表：

- `批量原创`：真正导入的数据表，一行一首歌。
- `填写说明`：字段含义、默认值和填写规则。

仓库模板还带有模型、人声、账号、启用等下拉选项，建议直接复制模板后填写，不要随意改动表头。

软件里的“下载模板”按钮也会生成同一套字段结构；仓库中的模板作为版本固定样例，方便以后从 GitHub 直接下载和核对。

---

## 3. Excel 格式

程序读取**第一张工作表**，第一行为表头，从第二行开始是一首一行的数据。

标准列顺序：

```text
歌名 | 歌词 | 风格 | 排除风格 | 模型 | 人声 | Weirdness | 风格影响 | 账号 | 提交间隔秒 | 启用
```

| 列名 | 必填 | 说明 | 默认值 / 允许值 |
|---|---|---|---|
| 歌名 | 是 | 原创歌曲标题 | 无 |
| 歌词 | 是 | 完整歌词；单元格内可以换行 | 无 |
| 风格 | 否 | Suno style prompt | 空 |
| 排除风格 | 否 | 作为 `negative_tags` 提交 | 空 |
| 模型 | 否 | Suno 模型 | `v5.5`；支持 `v5.5` / `v5` / `v4.5+` / `v4.5-all` |
| 人声 | 否 | 人声性别 | 不指定；支持 `不指定` / `女声` / `男声` |
| Weirdness | 否 | Weirdness 滑杆 | `50`；范围 0-100 |
| 风格影响 | 否 | Style Influence | `50`；范围 0-100 |
| 账号 | 否 | 使用账号位 | `1`；支持 1 / 2 / 3 |
| 提交间隔秒 | 否 | 当前歌曲提交结束到下一首开始之间的等待时间 | 空时使用软件全局值；5-300 秒 |
| 启用 | 否 | 是否提交这一行 | 默认“是”；填“否”直接跳过 |

### 3.1 最简单填写方式

如果所有歌曲都使用默认参数，实际只需要填写：

```text
歌名 + 歌词
```

其他列留空即可，程序会使用默认值。

### 3.2 表头兼容

除了标准中文表头，当前代码还兼容部分别名：

- 歌名：`歌曲名` / `title`
- 歌词：`lyrics`
- 风格：`风格提示词` / `style` / `stylePrompt`
- 排除风格：`negativeStyle` / `negative style` / `negative_tags`
- 模型：`model` / `modelVersion`
- 人声：`性别` / `gender` / `vocalGender`
- 账号：`账号位` / `slot`
- 提交间隔秒：`间隔秒` / `等待秒数` / `intervalSeconds`
- 启用：`是否提交` / `enabled`

虽然兼容别名，正式使用仍建议直接采用仓库模板的中文标准表头。

### 3.3 值兼容

模型也接受 `5.5`、`5`、`4.5+`、`4.5-all` 等写法，程序会转换为内部版本名。

人声也兼容 `female/f`、`male/m`、`none/auto`；正式模板建议继续填写中文“女声 / 男声 / 不指定”。

`启用` 填 `否`、`0`、`false`、`no`、`禁用`、`跳过` 都会被视为不提交；空白默认启用。

完全空白的行不会进入队列。

---

## 4. 导入和提交流程

```text
选择 Excel
→ 读取第一张工作表
→ 每行转换为一个任务
→ 参数校验 + 队列预览
→ 开始批量
→ 检查当前行指定账号状态
→ checkpoint：submitting
→ 提交当前歌曲
→ [如需要] Suno 官方人机验证
→ Suno 返回两个 clip ID
→ 写入歌曲列表
→ checkpoint：submitted
→ 等待 N 秒
→ 下一行
```

当前版本是**严格顺序队列**：不会一次并发提交多首。只有当前歌曲的提交动作结束后，才开始计算等待时间，等待结束后再进入下一首。

如果 Excel 某一行填写了“提交间隔秒”，该行的值优先；没有填写时使用软件顶部的全局间隔，默认 20 秒。

---

## 5. 队列状态

每一行可能出现以下状态：

| 状态 | 含义 |
|---|---|
| `queued` | 已校验，等待提交 |
| `submitting` | 正在调用 Suno 提交接口 |
| `submitted` | 已成功取得 clip ID，并写入歌曲列表 |
| `error` | 提交失败，可人工重试 |
| `invalid` | Excel 参数不合法，不会提交 |
| `skipped` | `启用=否`，主动跳过 |
| `interrupted` | 程序在 `submitting` 阶段退出，需要人工确认 |

批量页面会显示总数、待提交、已提交、失败、中断和参数错误数量，同时逐行显示账号、模型、间隔、状态和错误信息。

---

## 6. 暂停、继续和重试

### 暂停

点击“暂停”后，不会粗暴终止正在提交的当前歌曲，而是等待当前歌曲提交动作结束，再安全停止后续队列。

### 继续

暂停后可以重新点击“继续批量”，从仍为 `queued` 的下一首继续。

### 重试失败 / 中断

`error` 和 `interrupted` 可以重新放回队列，但对 `interrupted` 必须先人工检查 Suno：

> 如果程序在 Suno 已经接收任务之后、客户端还没来得及保存 clip ID 时退出，直接重试可能产生重复生成。

因此程序不会自动重提 `interrupted` 项。

---

## 7. 程序重启后的恢复规则

批次状态当前保存在主界面的 `localStorage`，键为：

```text
suno-batch-v058
```

程序重新打开时：

- 上次 `running` 的批次会自动变为 `paused`。
- 上次仍为 `submitting` 的行会自动变为 `interrupted`。
- 已经 `submitted` 的行不会再次提交。
- `interrupted` 不自动重新提交，需要人工确认后再点重试。

这是为了避免重复生成，而不是为了把所有异常都自动吞掉。

---

## 8. 账号规则

v0.5.8 的 Excel 可以在每行指定账号 `1 / 2 / 3`。

当前版本**还没有**自动 round-robin，所以：

- Excel 写账号 1，就使用账号位 1。
- Excel 写账号 2，就使用账号位 2。
- Excel 写账号 3，就使用账号位 3。
- 账号列留空默认账号 1。

如果当前行指定的账号未登录、登录失效或额度不足，批量队列会暂停，避免后续连续失败。

3 账号自动轮流、账号 busy 锁、多个账号同时处理不同歌曲、一个账号验证时其他账号继续运行，放在后续 v0.6.x。

---

## 9. Suno 官方验证

批量任务复用单首原创现有的官方验证链路。

如果某首歌提交时 Suno 要求人机验证：

```text
当前歌曲暂停在提交阶段
→ 打开 Suno 官方 hCaptcha / Cloudflare Turnstile
→ 用户完成官方验证
→ 当前歌曲自动继续
→ 成功后再进入等待间隔
→ 下一首
```

程序不识别、不代答、不破解验证码，也不会伪造验证 token。

---

## 10. 与歌曲列表 / WAV / N19 的关系

Excel 只负责批量**提交原创任务**。每一行提交成功后，会调用已有的歌曲列表保存逻辑，因此两个 Suno 版本都会正常进入歌曲列表。

后续继续走原来的自动流水线：

```text
Excel 提交成功
→ 两个 clip 写入歌曲列表
→ 后台轮询 Suno 生成状态
→ complete
→ [自动下载开关] 下载 Suno WAV + 保存歌词
→ [自动 N19 开关] AVR 1.77.0 exact N19
→ 本地作品目录
```

所以批量原创不会另外复制一套下载/N19 逻辑。

---

## 11. v0.5.8 当前边界

已经完成：Excel 导入、参数校验、预览、顺序逐首提交、每首等待间隔、暂停/继续、失败重试、本地 checkpoint、异常退出保护、成功写入歌曲列表。

暂未完成：3 账号自动轮流、多账号并行调度、单账号 busy 锁、账号级独立暂停、批量结果 Excel 导出、多批次历史管理。

这些进入后续 v0.6.x。
