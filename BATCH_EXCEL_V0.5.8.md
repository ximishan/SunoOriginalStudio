# v0.5.8 Excel 批量原创

## 目标

v0.5.8 增加 Excel 顺序批量原创：上传一个 `.xlsx/.xls` 文件，一行代表一首歌曲，程序按照 Excel 顺序逐首调用现有 Suno 原创提交链路。

每首提交结束后会等待指定秒数，再进入下一首。默认等待 **20 秒**，可在软件中统一设置，也可在 Excel 的“提交间隔秒”列为单首覆盖。允许范围为 5-300 秒。这个间隔用于控制请求频率；不会绕过、破解或代答 Suno 的官方验证。

## Excel 格式

第一张工作表作为数据表，第一行为表头。

| 列名 | 必填 | 说明 | 默认值 |
|---|---|---|---|
| 歌名 | 是 | 原创歌曲标题 | - |
| 歌词 | 是 | 完整歌词；单元格可以换行 | - |
| 风格 | 否 | Suno style prompt | 空 |
| 排除风格 | 否 | 提交为 `negative_tags` | 空 |
| 模型 | 否 | `v5.5` / `v5` / `v4.5+` / `v4.5-all` | `v5.5` |
| 人声 | 否 | `不指定` / `女声` / `男声` | 不指定 |
| Weirdness | 否 | 0-100 | 50 |
| 风格影响 | 否 | 0-100 | 50 |
| 账号 | 否 | 1 / 2 / 3；本版按每行指定账号，不自动轮流 | 1 |
| 提交间隔秒 | 否 | 当前行结束后到下一行之前的等待时间，5-300 秒 | 软件全局值，默认 20 |
| 启用 | 否 | `是/否`；填“否”的行直接跳过 | 是 |

程序也兼容部分英文别名，例如 `title`、`lyrics`、`style`、`negativeStyle`、`model`、`gender`、`slot`。

## 批量流程

```text
选择 Excel
→ 解析第一张工作表
→ 参数校验 + 预览
→ 开始批量
→ 检查当前行账号状态
→ 提交当前歌曲
→ Suno 官方验证（如需要）
→ 保存两个 clip 到歌曲列表
→ checkpoint 当前行结果
→ 等待 N 秒
→ 下一行
```

每一首成功提交后仍然进入原有“歌曲列表”，因此后续继续使用已有流水线：

```text
歌曲列表后台轮询
→ 生成完成
→ [可选] 自动下载 WAV + 保存歌词
→ [可选] 自动 AVR N19
```

## 队列与安全恢复

- 支持开始、暂停、继续。
- 支持把失败行重新放回队列。
- Excel 批次和每行状态保存到主界面的 `localStorage`，程序重启后仍能看到队列。
- 每行调用 Suno 前先写成 `submitting`；成功返回 clip ID 后再写 `submitted`。
- 如果程序恰好在提交过程中被关闭，该行重启后标记为“中断待确认”，**不会自动重新提交**，避免重复生成。
- “重试失败/中断”会明确提示先确认 Suno 中是否已经存在对应生成任务。
- 遇到账号未登录、登录失效或额度不足时，整个顺序队列自动暂停，避免后续连续失败。

## 本版范围

v0.5.8 是**单队列顺序提交**。Excel 可以为每一行指定账号 1/2/3，但还没有做 3 个账号自动 round-robin 并发调度、单账号 busy 锁和“一个账号验证时其他账号继续跑”。这些仍放在后续 v0.6.x。
