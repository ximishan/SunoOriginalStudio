# v0.5.6 统一 Session/Auth 与后台自动流水线

## 目标

v0.5.6 完成两个 P0 项：

1. 将 `main.js`、`song_library.js`、账号状态检测统一到共享 Suno Session/Auth 模块。
2. 将“手动刷新 → 手动下载 WAV → 手动 N19”扩展为可恢复的后台自动流水线。

## 统一 Suno Session / Auth Token

新增 `suno_session.js`，统一提供：

- `partitionFor(slot)` / `sessionFor(slot)`
- `getAccountStatus(slot)`
- `getAuthToken(slot)`
- `apiHeaders(slot)`
- `flushAccountSession(slot)` / `flushAllAccountSessions()`

Token 获取不再要求短生命周期 `__session` Cookie 必须预先存在。程序会在对应 persistent partition 中恢复 Suno 页面与 `window.Clerk.session`，再调用 `Clerk.session.getToken()` 获取当前有效 token。只有 Clerk Session 确实无法恢复时才判定账号登录失效。

三个账号继续使用独立 partition：

```text
persist:suno-original-demo-1
persist:suno-original-demo-2
persist:suno-original-demo-3
```

## 后台自动流水线

程序启动后，主进程会启动歌曲流水线定时器。默认每 5 秒检查一次未完成歌曲；轮询在主进程运行，不依赖“歌曲列表”页面是否打开。

```text
提交原创
→ 持久化歌曲列表
→ 后台按账号轮询 /api/feed/v2
→ complete
→ [自动下载开关] convert_wav → wav_file → 下载 WAV + 保存歌词
→ [自动 N19 开关] AVR 1.77.0 exact N19
→ 本地作品目录
```

### 自动化开关

歌曲列表新增：

- `生成完成后自动下载 WAV + 保存歌词`
- `下载完成后自动 AVR N19`
- `立即运行一次`

开启“自动 AVR N19”时会自动开启 WAV 下载，因为 N19 必须以 Suno WAV 为输入。

## 幂等与重启恢复

自动化配置与歌曲状态继续保存在 `%APPDATA%\SunoOriginalStudio\song-library-v1.json`。状态结构升级为 version 2，但沿用原文件名以兼容旧数据。

自动处理执行前同时检查：

- 数据状态（`wavStatus` / `deaiStatus`）
- 目标文件是否已经存在且大小大于 0

因此程序关闭或异常退出后再次启动：

- 已下载 WAV 不重复下载
- 已完成 N19 且文件存在时不重复消痕
- 未完成的生成任务继续轮询
- 已开启的自动化开关继续生效

## 错误与退避

单首歌曲自动处理失败后不会高频重试。运行时使用指数退避：约 5 秒、10 秒、20 秒、40 秒……最高约 120 秒后再尝试。错误会写入歌曲 `lastError` 并在歌曲列表中显示。

## 人机验证边界

本版本没有增加验证码识别、绕过、代答或伪造 token。原创提交的人机验证仍只使用 Suno / Cloudflare / hCaptcha 官方组件与官方回调链路。
