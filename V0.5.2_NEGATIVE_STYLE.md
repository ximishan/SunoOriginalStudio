# v0.5.2 排除风格参数

本版本在原创歌曲提交页新增独立的“排除风格”输入框。

## 已实现

- 原创页面新增 `排除风格` 字段。
- 前端字段名：`negativeStyle`。
- 提交 Suno 时单独映射到生成接口 payload 的 `negative_tags`，不会混入普通风格 `tags`。
- 支持填写多个不希望出现的风格、元素或演唱方式，例如：`rap, trap, metal, spoken word, choir`。
- 歌曲提交后，`negativeStyle` 同步写入本地歌曲列表数据，方便后续追溯生成参数。
- 保留 v0.5.1 的稳定登录 Session、歌曲列表、Suno WAV 下载和 AVR 1.77.0 N19 完整处理链路。

## 后续批量原创

后续增加批量原创/Excel 导入时，同样使用独立的“排除风格”列，并传入同一个 `negativeStyle -> negative_tags` 参数链路。
